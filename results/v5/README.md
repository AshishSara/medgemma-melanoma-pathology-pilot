# V5 result namespace

V5 is terminal incomplete and unscored.

The development namespace contains the preserved result of a Colab qualification launch that
stopped during gated-model backend access with HTTP 401. No model call, candidate response, audit
response, clinical JSON output, or ground-truth scoring occurred.

See `docs/v5_access_incident.md` for the incident record and `docs/restart_handoff.md` for the
future restart boundary.
